-- Добавляет значение 'blocked' в enum seat_status, чтобы админ мог вручную
-- заблокировать место (например, техническая неисправность), не создавая
-- фиктивный заказ. Требуется для новой админ-панели (управление местами).
-- Безопасно выполнять повторно.
ALTER TYPE seat_status ADD VALUE IF NOT EXISTS 'blocked';
